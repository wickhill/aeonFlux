<!DOCTYPE html>
<html lang="en">
<head>
    <head>
        <%- include('./partials/head.ejs', {title: 'Show Page'}) %>
    </head>
    
</head>
<body>
    <nav>

        <!-- REMEMBER TO ALTER THE TAGS BELOW WHEN YOU WANT TO USE THEM!!  -->
        <%- include('./partials/nav.ejs') %>
    
    </nav>
    <h1>Citizens Show Page</h1>
    <p>
    <% if(citizen){ %>

      Your report on <span style="font-size: 18px;"><%= citizen.name.toUpperCase() %></span> related to the charge of <span style="font-size: 18px;"><%= citizen.charges.toUpperCase() %></span> has been successful. Thank you for doing your part <span style="font-size: 18px;"> Citizen <%= currentUser._id.toUpperCase() %></span>.

        <% if(citizen.requiresInvestigation === true) { %>
            <span>Investigation will commence in due time.</span>
        <% } else { %>
            <span>Investigation not necessary at this time.</span>
            <% }%>
            
    <% }else { %>
        
        <h1>Citizen at that index does not exist</h1>

    <% } %>
</p>
<div class="showPageContainer">
    <a href="/citizens/<%= citizen._id %>/edit">Provide Information regarding this Citizen</a>
    <a href="/">Home</a>
</div>


    <%- include('./partials/footer.ejs') %>
</body>
</html>